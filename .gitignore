# Credentials -- never commit these. Copy .env.example to .env and fill
# in real values; .env.example itself is safe to commit (no secrets).
.env
.env.*
!.env.example

# Dev tooling only (Prettier + PHP plugin) -- no build step for the site itself.
node_modules/
